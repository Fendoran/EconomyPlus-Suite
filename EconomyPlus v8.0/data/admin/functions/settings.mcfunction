## The Basis of all admin settings (terminal UI)
scoreboard players operation #ep.target_uid ep.session = @s ep.session
function directory:terminal/remove

execute rotated ~ 0 anchored eyes positioned ^ ^ ^1.7 run function admin:settings/home/main
